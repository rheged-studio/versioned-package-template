// @generated by `pnpm vendor:sync` from lib/base-branch.mjs — DO NOT EDIT.
// Canonical source: lib/base-branch.mjs. CI `pnpm vendor:check` fails on drift (ADR-0004).

// Canonical base-branch detection — the single source of truth vendored into
// every bundle that resolves the repo's default branch (preflight,
// initialise-skills). See ADR-0004 (A-534): bundles stay independently
// installable, so `pnpm vendor:sync` copies this file into each consumer's
// `scripts/lib/vendor/` rather than them importing across bundles.
//
// Edit THIS file; never the vendored copies (the CI `pnpm vendor:check`
// drift-gate rejects an out-of-band edit).

import { spawnSync } from "node:child_process";

/**
 * Resolve the repo's default branch from `origin/HEAD` (e.g. `main`, `master`,
 * `develop`), falling back to `defaultBaseBranch` when the symbolic ref is absent
 * (common on fresh clones / shallow CI checkouts).
 * @param {string} root
 * @param {string} [defaultBaseBranch]
 * @returns {string}
 */
export function detectBaseBranch(root, defaultBaseBranch = "main") {
  const result = spawnSync(
    "git",
    ["symbolic-ref", "--quiet", "refs/remotes/origin/HEAD"],
    { cwd: root, encoding: "utf8" },
  );
  if (result.status === 0 && result.stdout.trim()) {
    return result.stdout.trim().replace(/^refs\/remotes\/origin\//, "");
  }

  return defaultBaseBranch;
}
