name: CI

on:
  pull_request:
    # `edited` is included so the changelog-completeness gate re-runs when a
    # title is corrected — a release-triggering title must carry a changelog
    # entry (SK-371), so a stale pass on an old title would be unsafe. Feature
    # PRs land as merge commits; release-please ranks Conventional Commits on
    # `main` (A-824 / A-1176), so the PR title is no longer the sole post-merge
    # bump signal. (The PR-title lint itself now lives in validate-pr-title.yml,
    # which also re-runs on `edited`.) The default set is opened/synchronize/reopened.
    types: [opened, edited, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # A-1100 / A-1195: never cancel a RUNNING run. A superseded run's `needs` all
  # report `cancelled`, the always() aggregator still executes, and its verdict
  # mints a FALSE RED `GO/NO GO`. With `false`, concurrency can only cancel a
  # *pending* run (no check-runs minted). Subsumes the A-961 release-please
  # carve-out — covers every branch, not just the release one.
  cancel-in-progress: false

# Defence-in-depth (ASW-313): the repo default is already read-only, but
# pin it here so a future org-policy change can't silently widen CI's token.
permissions:
  contents: read

# The lint + build-test work runs through the estate's shared reusable
# workflows (SK-447, completing SK-424's deferred caller half); the template is
# the reference consumer that proves the pattern before the fleet rollout
# (SK-420). The callers float on the shared-workflows `@v1` major tag so
# non-breaking releases arrive without a per-release Dependabot SHA bump (A-662
# / A-672). PR-title validation now runs through its own `validate-pr-title.yml`
# caller (A-695), leaving only the changelog-completeness gate inline here (it
# reads the PR title, which a reusable workflow can't). The `GO/NO GO`
# aggregator (SK-412/SK-424) stays local and unchanged — it is the
# estate-canonical required check and lives per-repo by design.
jobs:
  # No `config` (load-repo-config) job. A deploy target carries no npm
  # registry/scope keys, and the estate's reusable-load-repo-config@v1 hard-
  # requires them (npmRegistryUrl / npmScope / githubPackagesRegistryUrl) — it
  # aborts against a content-only repo-config.yaml (A-941). The reusable callers
  # default `node-version-file` to `.nvmrc`, so each job points at `.nvmrc`
  # directly (the same file setup-node reads). `infrastructure/repo-config.yaml`
  # stays as a declarative record (defaultBranch + nodeVersionFile) maintained by
  # the initialise-versioned-repo skill, not loaded by CI.

  # ESLint + Markdown + YAML/actionlint + dated-changelog validation, via the
  # shared Layer-2 lint bundle. Renders the status context `lint / Lint`.
  # Deliberately NOT skipped on the release-please branch — the changelog lane
  # must validate entries (including any post-merge enricher write-backs that
  # land on main) via `pnpm exec changelog-core validate` (A-808).
  lint:
    uses: rheged-studio/shared-workflows/.github/workflows/reusable-lint.yml@v1
    with:
      node-version-file: .nvmrc
      # ESLint re-scoped off the (removed) `src/` onto the repo's first-party
      # infrastructure code (A-940). The content-only baseline has no `src/`, but
      # `infrastructure/tests/**` holds first-party `.mjs` — the repo-local
      # init-skill test suites — worth gating in CI. Scoped to
      # `infrastructure/tests` rather than the whole `infrastructure/` tree on
      # purpose: the shared eslint-config bundle carries YAML rules that would
      # double-lint (and fight) `repo-config.yaml`, which the yaml lane below
      # already owns; and `infrastructure/scripts` is shell-only (eslint errors
      # when every file under a passed directory is ignored). `eslint-args` takes
      # directory paths, not globs — globstar is off in the Layer-1 runner.
      eslint: true
      eslint-args: infrastructure/tests
      # Match the `lint:md` script: include .mdx and the dist/turbo/astro
      # excludes the estate default omits.
      markdown-globs: |
        **/*.{md,mdx}
        !**/node_modules/**
        !**/dist/**
        !**/.turbo/**
        !**/.astro/**
      # The template's validator script is `validate:changelog` (the reusable
      # default is `changelog:validate`) — implemented by changelog-core.
      changelog-script: validate:changelog
      # yaml lane defaults are correct: yaml-paths `.`, yamllint 1.37.1
      # (matches), actionlint 1.7.12 (owned upstream in lockstep, SK-422 — do
      # not override per-consumer). yamllint uses shared-workflows' centralised
      # .yamllint.yml (SK-438), so the repo's local .yamllint.yml is now only
      # consumed by the pre-commit hook.

  # Build (verification) + Vitest + ShellCheck + bats, via the shared Layer-2
  # build/test bundle. Renders the status context `build-test / Build & Test`.
  # Absorbs the old build-and-lint `build` step plus the entire `infra` job.
  build-test:
    uses: rheged-studio/shared-workflows/.github/workflows/reusable-build-test.yml@v1
    with:
      node-version-file: .nvmrc
      # Build + typecheck are off: the A-939 content-only baseline has no `src/`
      # to compile (the tsconfig trio and the `build`/`tsc` scripts were removed),
      # so there is no verification build or type-check to run. Vitest, ShellCheck
      # and bats below remain the build-test gate.
      build: false
      typecheck: false
      # Mirror the `lint:sh` script: the action `find`s *.sh/*.bash under a
      # directory but takes files literally, so pass the scripts dir plus the
      # three extensionless husky hooks explicitly (a `.husky` dir arg would
      # find none of them). Default severity `style` matches bare shellcheck.
      shellcheck-paths: "infrastructure/scripts .husky/pre-commit .husky/pre-push .husky/commit-msg"
      # Opt-in (off by default). bats runs via `pnpm exec bats`, so `bats` is a
      # devDependency (bats@1.13.0, matching ensure-bats.sh's pin). bats-paths
      # defaults to infrastructure/tests, recursive.
      bats: true

  # Changelog-completeness gate (SK-371): a release-triggering PR title
  # (feat/fix/breaking) must carry a dated changelog/ entry. Restores the
  # no-entry-no-release coupling for titles that still signal shippability at
  # PR time. Post-merge, release-please ranks Conventional Commits on `main`
  # (A-824 / A-1176) — the PR title is no longer the sole bump signal.
  # Non-release titles (docs/chore/ci/…) — including the release-please PR's
  # own `chore: release …` — are a clean pass. Stays inline because it reads
  # the PR title (github.event.pull_request.title), which a reusable workflow
  # can't see; was a step inside the old build-and-lint job.
  changelog-completeness:
    name: 📓 Changelog completeness
    runs-on: ubuntu-latest
    env:
      HUSKY: 0
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Full history: the gate diffs against origin/<base_ref>.
          fetch-depth: 0
          # Local diff only (no runtime fetch), so the token isn't needed after
          # checkout; don't persist it (zizmor artipacked).
          persist-credentials: false

      - name: 📦 Install pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v4.4.0
        with:
          run_install: false

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 📓 Changelog completeness
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          BASE_REF: ${{ github.base_ref }}
        run: pnpm exec changelog-core check-completeness

  # Single estate release gate (SK-412/SK-424). An always()-run aggregator whose
  # intrinsic check-run, named `GO/NO GO`, succeeds iff every required job above
  # passed or legitimately skipped. A check-run (not a commit status) is
  # App-only — minted solely by this repo's own Actions run — so a push-scoped
  # token or a fork contributor cannot forge it; require it on `main` via a
  # ruleset pinned to the GitHub Actions integration. The literal `/` and space
  # in the job name surface verbatim as `check_run.name == "GO/NO GO"` (emoji and
  # spaces already survive in `lint / Lint`); if it ever doesn't, fall back to
  # explicit-create (POST /check-runs, Option A).
  #
  # The caller swap SK-411 foreshadowed has now landed (SK-447): the lint /
  # build-test work runs through rheged-studio/shared-workflows reusable
  # workflows; this aggregator stays put across that swap, which is exactly why
  # it lives here (custom per-repo) and not upstream.
  go-no-go:
    name: GO/NO GO
    needs: [lint, build-test, changelog-completeness]
    if: ${{ always() }} # MANDATORY — else the aggregator skips and the gate never reports
    runs-on: ubuntu-latest
    steps:
      - name: ⚖️ Verdict
        # Pass/fail by inspecting the `needs` results (A-1103 / A-1195). `skipped`
        # is allowlisted only on `release-please--*` branches (path-skipped jobs);
        # everywhere else every needed job must report `success`. `failure`,
        # `cancelled` and a null/empty result fail the gate.
        #
        # `cancelled` is deliberately NOT allowlisted (A-1100). NEVER reach for
        # `if: !cancelled()` on this job — a skipped check-run counts as SUCCESS
        # for required status checks and would forge the gate.
        env:
          # Read through `env:`, never interpolate into the shell body: job
          # names reach this context from the workflow and would otherwise be a
          # template-injection surface.
          NEEDS_JSON: ${{ toJSON(needs) }}
          HEAD_REF: ${{ github.head_ref }}
        run: |
          set -euo pipefail
          # Allow `skipped` only on release-please path-skips; otherwise every
          # needed job must have succeeded (A-1103).
          if [[ "$HEAD_REF" == release-please--* ]]; then
            ALLOWED='["success","skipped"]'
          else
            ALLOWED='["success"]'
          fi
          echo "$NEEDS_JSON" \
            | jq -e --argjson ok "$ALLOWED" \
              'to_entries | all(.value.result as $r | $ok | index($r))' >/dev/null \
            || { echo "::error::a required check failed — refusing to go"; exit 1; }
          echo "All required checks passed — GO."
